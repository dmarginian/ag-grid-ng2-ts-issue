{
  "name": "ag-grid-issue",
  "version": "1.0.0",
  "description": "Illustrates a TypeScript Issue that arises with ag-grid-ng2 due to ag-grid-ng2 publishing typescript files",
  "author": "dmarginian",
  "main": "dist/index.js",
  "scripts": {
    "install": "npm run clean && npm run tsc",
    "clean": "rimraf dist",
    "tsc": "tsc -p ./src/tsconfig.json"
  },
  "dependencies": {
    "@angular/core": "2.x",
    "ag-grid": "7.x",
    "ag-grid-ng2": "7.x",
    "core-js": "^2.4.1",
    "rxjs": "5.0.1"
  },
  "devDependencies": {
    "typescript": "2.0.x",
    "rimraf": "2.x",
    "copyfiles": "1.x",
    "globstar": "1.x"
  }
}
